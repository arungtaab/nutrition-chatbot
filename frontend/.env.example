# Copy to .env.local (not committed). Default matches local Flask.
VITE_API_BASE_URL=http://localhost:5000

# Production: set to your deployed API origin before `npm run build`, e.g.
# VITE_API_BASE_URL=https://your-api.onrender.com
